// This is a comment
let name = "John Doe";
console.log(name);

// Arrow function expression
const add = (a: number, b: number): number => {
  return a + b;
}

// Object destructuring
const person = { name: "John", age: 30 };
const { name } = person; // name is now equal to "John"
